# Copy this file to `.streamlit/secrets.toml` for local testing.
# On Streamlit Community Cloud, add the same values in the app settings.

APP_NAME = "chat_Ai_Tool"
APP_DEBUG = false
SECRET_KEY = "change-me-to-a-long-random-secret"
DATABASE_PATH = "data/app.db"
SESSION_TTL_MINUTES = 120
GEMINI_API_KEY = ""
GEMINI_MODEL = "gemini-1.5-flash"
GEMINI_TIMEOUT_SECONDS = 30.0
AI_SYSTEM_PROMPT = "You are a helpful private chat assistant. Keep replies concise, respectful, and useful."
